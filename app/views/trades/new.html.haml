.trades-new
  %header.trades-new-header
    = image_tag 'logo.png'
  %main.trades-new-main
    // ログイン、サインアップ、ログアウトボタン
    = link_to "login", new_user_session_path, class: "btn btn-primary"
    = link_to "signup", new_user_registration_path, class: "btn btn-primary"
    = link_to "signout", destroy_user_session_path, method: :delete, class: "btn btn-primary"

    // 現在ログインしているユーザーIDの表示
    - if user_signed_in?
      現在、ユーザー#{current_user.id}でログイン中です
    - else
      未ログインです  
  %footer.trades-new-footer